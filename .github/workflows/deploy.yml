# name: Deploy to GitHub Pages

# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:

# permissions:
#   contents: read
#   pages: write
#   id-token: write

# # ⬇️ กันงานซ้อน/ยกเลิกเฉพาะงานเก่า (ไม่ขึ้น cancelled เกะกะ)
# concurrency:
#   group: "pages"
#   cancel-in-progress: true

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20

#       - name: Install & Build
#         run: |
#           npm ci
#           npm run build
#           # ถ้าโปรเจ็กต์ build ออกเป็น dist ให้ย้ายเข้า docs
#           if [ -d dist ]; then
#             rm -rf docs
#             mkdir -p docs
#             cp -r dist/* docs/
#             cp docs/index.html docs/404.html || true
#             touch docs/.nojekyll
#           fi

#       - name: Upload Pages artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: ./docs

#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
